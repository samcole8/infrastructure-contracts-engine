[project]
name = "infrastructure-contracts-engine"
version = "0.0.1"
description = "Infrastructure Contracts Engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Fabric==3.2.3",
]

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
ice = "ice.main:main"
